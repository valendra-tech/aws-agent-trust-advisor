# AWS configuration is loaded from ~/.aws/config and ~/.aws/credentials
# Use the --profile flag to specify which AWS profile to use
# Ensure Bedrock is enabled in your AWS account and you have access to the models
